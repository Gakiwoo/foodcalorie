# 2026-08-18 食刻工作日志

## 按 MasterGo 设计稿还原前端页面
- 对比 `.mastergo/design/200862263389388/M/*.html` 与当前 10 个核心页面实现
- 生成差异报告 `docs/design-diff-report-2026-08-18.md` 与最终验证报告 `docs/design-final-verify-2026-08-18.md`
- 完成修改并提交 `22d5b35`

### 修改范围
- 全局组件：`src/ui/common.jsx`（StatusBar Web 伪状态栏、BottomNav 顺序/视觉、NavBar dark/隐藏返回、Ring 白心）
- 10 个核心页面：Home / Me / Records / Today / Camera / CameraResult / AddFood / Search / Challenge / Detail
- 测试适配：`nativeChrome.test.js`、`homeStats.test.js`
- 文档索引：`README.md` 新增设计报告链接

### 验证
- `npm run lint`：通过
- `npm run build`：成功
- `npx vitest run`：14/14 通过
- 生产 E2E（verify_prod.cjs）：7/7 通过
- 已部署前端到 `/var/www/foodcalorie-web`（备份 `foodcalorie-web-backup-20260818`）
- 已推送 GitHub + Codeup：`21420a7`

### 遗留
- 未覆盖页面：Settings / Login / Register / Profile / Help / DietPref / Unit / Precision / Burst / RecordsWeek / RecordsMonth / About / Privacy / Notification / DataExport / Favorites / Goal / Article / Recipe / Discover / EditRecord
- AddFood 常见食物缩略图仍为通用 FontAwesome 图标，可按食物类型替换为独立 SVG

## 项目全面评估（下午）
- 双 Agent 深度审查（后端 37 文件 + 前端全部页面）+ 实测验证，报告 `docs/project-assessment-2026-08-18.md`
- 结论：**达到构建/发布标准**（0 P0；4 P1 全前端；22 P2）
- P1：RecordsMonth isToday 恒 false（date 为 MM-DD vs todayStr 函数引用）、Home/Today record_time.slice 无空值防护、client.js hadSession 恒 false（tokenStore.set 无调用）、APK 跨源 cookie 待真机验证
- P2 关键：multer 错误未映射 413、CSV \t\r 注入、JWT algorithms 未显式、SIGTERM 缺失、body 日期校验、单 bundle 97KB 无代码分割
- 验证项：audit 0 漏洞、CI 4 job 全绿、45 后端单测 + 14 前端 + 生产 E2E 7/7
- 提交 `310c852` 推送 GitHub + Codeup

## 评估问题修复（下午，commit f9fbead + 29954d2）
- 修复全面评估发现的 4 P1 + 22 P2 并部署生产、推送四端
- P1：RecordsMonth isToday 修复、Home/Today record_time 防护、client markSession/hasSession 会话标记（401 跳转修复）、auth 同步
- 后端 P2：multer 错误 413/400 映射、CSV \t\r 注入、JWT algorithms HS256、SIGTERM 优雅关闭、body 日期校验（record_time/birthday）、page 上限 10000、imageStore 目录一次性创建 + pending 6h 清理 + 单用户 30 上限、删死代码 listRecords/updateLastCheckIn
- 前端 P2：Settings 真实数据、Profile 错误提示、DataExport 本地日期、NaN 防护、normalizeDailyStats 接入 Records/Today、触控热区、AddFood safe-area、Switch 收敛 ToggleSwitch(onChange)、**React.lazy 路由级代码分割（主包 362KB→200KB / gzip 66KB）**
- 验证：后端单测 45/45（服务器）、前端 lint/14 测试、生产 E2E 7/7、构建成功
- 生产已部署（备份 *-backup-20260818b）；APK 登录态真机联调 + Redis 限流为剩余待办

## 剩余待办处理（傍晚，306cc00→1d5e8d6）
- **Redis 可插拔限流**：rateLimitStore.js（REDIS_URL→Redis Lua 滑窗，未配置/故障回退内存），rateLimit.js 内存路径保持同步（测试兼容）；生产启用 REDIS_URL=redis://127.0.0.1:6379，单测 45/45 + Redis 路径实测 429
- **APK v1.0.4**：MainActivity 加 setAcceptThirdPartyCookies（跨源登录态兜底）、versionCode 5；本机 gradle 因文件锁（safe-delete shim + .lck/native-platform.dll 失败）无法构建 → 改 CI：ci.yml android-debug 加 upload-artifact@v4；修复 capacitorConfig.test.js 断言（versionCode 4→5）；CI 4 job 全绿，下载 app-debug.apk（4.9MB）→ 食刻-app-debug-v1.0.4.apk
- **服务器备份清理**：删 20260811/20260817 系列 5 个旧备份，保留今日 4 份（api-b/c、web/b）
- **本机文件锁问题**（记录）：safe-delete shim 对 rm/mv/cp 覆盖/部分目录 fail-closed；public/index.html、gradle .lck 被锁；robocopy 曾成功更新 public（dist-apk 与 public 已一致）；后续本机 APK 构建建议直接走 CI
- 文档：评估报告补「剩余待办处理」章节

## ⚠️ 部署拓扑重大修正（傍晚，4b807a0）
- **发现**：pm2 foodcalorie-api 实际运行目录是 `/var/www/foodcalorie-releases/20260813T091015Z-b9cd7f4d7490/backend`（8/13 发布代码），**不是** `/var/www/foodcalorie-api`！
- **影响**：此前几轮只 scp 到 foodcalorie-api 的后端修复（SIGTERM/multer/JWT/日期校验/Redis 限流等）生产未生效
- **修复**：rsync foodcalorie-api/src → releases/backend/src（含 package.json/lock），releases .env 追加 REDIS_URL，npm install + pm2 restart
- **验证**：health 200、Redis 限流键 `limiter-3:127.0.0.1` 写入（生产走 Redis）、生产 E2E 8/8 全绿
- **教训（重要）**：**部署后端必须同步到 releases 目录（pm2 实际运行处），不能只更新 foodcalorie-api**；或改用 `pm2 describe foodcalorie-api` 确认 script path
- E2E：verify_prod 截图目录改 os.tmpdir()（archive 路径 EPERM）

## README 更新 + GitHub Release v1.0.4（傍晚，a401d56→cadd4f9）
- README：预览截图换 v1.0.4 生产实拍（docs/images/*-v2.png，snapshot-readme.cjs 生成）；新增「下载与安装」章节；构建章节改 CI/本机双路径 + VITE_API_ORIGIN 修正 + JDK 21；多端仓库标注 releases 实际目录；环境变量补 REDIS_URL；debug 签名表述修正
- GitHub Release：`v1.0.4`（标题「食刻 FoodCalorie v1.0.4」，tag 指向 a401d56），上传 `foodcalorie-v1.0.4-debug.apk`（4.9MB，命名规范 foodcalorie-v<版本>-debug.apk）；发布说明 docs/release-notes-v1.0.4.md；链接 https://github.com/Gakiwoo/foodcalorie/releases/tag/v1.0.4
- .gitignore 排除 *.apk（本地 APK 不入库，发布走 Releases）
- 后续版本发布：`gh release create v<版本> foodcalorie-v<版本>-debug.apk --title ... --notes-file docs/release-notes-v<版本>.md`（HTTP_PROXY= HTTPS_PROXY= 清空代理）
