# 2026-08-17 食记 FoodCalorie 工作日志

## 全栈 Bug 诊断修复 + 文档更新 + 多端推送部署

### 四端一致性（起始状态）
- 本地 main = GitHub origin/main = codeup/main = `ec3f6b38` ✅
- 服务器 API(`b9cd7f4`)/Web(`14048a9`) 落后 3 个 docs-only 提交，运行代码一致

### 诊断与修复（13 处，详见 docs/bugfix-2026-08-17.md）
后端 5 处：
- records: `month=2026-13` → 原 500（NaN RangeError），regex 收紧 01-12 → 400
- records/export: 新增共享 `isValidCnDate()`，`2026-02-31` 静默回滚错月 → 400
- challenges: join/checkIn 加 `assertWithinWindow()`，过期挑战原可无限打卡刷积分
- imageStore: `cleanupExpiredPending` unlink 失败（EPERM/EBUSY）原阻断上传 → 仅记日志
- server.js: 启动日志健康路径修正为 `/api/v1/foodcalorie/health`

前端 8 处：
- Me.jsx: `fetchMe()` 返回 user 对象，再取 `me.user` 恒 undefined → `setUser(me)`
- client.js: 401 刷新单飞（single-flight）；游客不再强制踢登录页（hadSession 判断）；豁免判断兼容 APK HashRouter（pathname+hash）
- AddFood/Search: 防抖搜索加序号守卫防旧响应覆盖
- RecordsMonth: 当月日均分母 min(今天,月末)
- RecordsWeek: 重试改重发请求；CameraResult: confidence `?? 0` 防 NaN%；App: 返回栈 lastIndexOf 截断去重

### 测试与部署
- 新增 7 回归用例，服务器（Node 20）单测 **45/45** 通过；本地前端 vitest 14/14
- 本地 Node 22 无法跑 better-sqlite3（NODE_MODULE_VERSION 141≠127），后端测试走服务器：git archive → scp → 临时目录 npm install + test
- 前端 build 坑：vite safe-delete 清 dist 被 trash shim 中止 → 先 `rm -rf dist` 再 build
- 部署：服务器备份 `foodcalorie-api-backup-20260817` / `foodcalorie-web-backup-20260817` → scp 同步源文件（保留 .env/data/uploads）→ pm2 restart → health 200 → 生产 E2E verify_prod.cjs 全过（登录/记录/搜索/添加/相机/无 JS 错误）

### 最终状态
- 四端一致：`3abff7f`（本地/GitHub/codeup/服务器 .release+RELEASE_SHA）
- 提交：`86f6019`（fix 13 处）+ `3abff7f`（docs：bugfix 报告 + README 测试数 38→45）
- 新文档：`docs/bugfix-2026-08-17.md`（已加入 README 文档索引）

### 补充轮（下午，commit 1e30efc + ade07ee）
- 修复上轮引入的 RecordsMonth 遮蔽 bug：局部 `const todayStr` 遮蔽 import 的 `todayStr()` 函数 → 月历页 TypeError 崩溃；eslint no-unused-vars 暴露，已修
- 清理 loginThrottle 死代码（食刻无登录路由，登录在 gakiwoo-api）
- 核查：multer 实际已是 2.2.0（无需升级，遗留待办过时）
- 新增 frontend/scripts/verify_month_fix.cjs（月历/周视图生产专项验证，含登录流程）
- 最终四端一致：`ade07ee`；生产 health 200 + verify_prod + 月历专项全过

### 遗留
- Redis 多实例限流（生产单实例正常，多实例部署时替换）

### APK 构建（傍晚）
- 用户选择 debug 签名方案（FC release keystore 密码未持久化，未选提供密码）
- 构建新 debug APK：vite mode=apk → 手动同步资产（cap sync 被 safe-delete shim 阻断，用 cmd rd /s /q 绕过）→ gradlew assembleDebug（JAVA_HOME 需临时指 JDK21，默认 JDK17 报"无效的源发行版：21"）
- 删除旧 APK：release/app-release.apk（8月13日 3.7MB）已删；debug 旧包被新包覆盖
- 产出：项目根 `食刻-app-debug-v1.0.3.apk`（4.9MB，versionCode 4 / versionName 1.0.3，CN=Android Debug 签名，内置 index-ZWnUp2ou.js）
- 环境事实更新：Android SDK 在 C:/Android；JDK21 在 C:/Program Files\Microsoft\jdk-21.0.10+7
