# 2026-08-06 工作日志

## 后端正式接入 + M2 前端联调（继续昨天任务）

### 服务器盘点关键事实（只读确认）
- gakiwoo-api 实际运行于发布目录 `/var/www/gakiwoo-releases/<ts>/api/`（pm2 dump 确认），`/var/www/gakiwoo-api` 是旧副本目录
- 真实 .env：`/etc/gakiwoo/api.env`（release 的 .env 符号链接指向它）；`/var/www/gakiwoo-api/.env -> /Users/wujiaqi/...` 是**失效残留链接**（macOS 路径）
- 真实共享库：`/var/lib/gakiwoo/gakiwoo.db`（pm2 env DB_PATH），已有 5 用户
- JWT payload = `{id, email, role}`，与我们 requireAuth 完全一致
- login 响应体只有 `{user}`，token 全在 httpOnly Set-Cookie（Secure + SameSite=Strict + Domain=.gakiwoo.com）；refresh 仅读 cookie
- csrfOriginGuard：GET/无 Origin/白名单放行；**`http://127.0.0.1:5173` 已在 ALLOWED_ORIGINS** → 本地代理联调无障碍

### 正式接入（已完成，未改动 gakiwoo-api 任何代码）
1. 备份：gakiwoo.db → `/var/backups/gakiwoo/gakiwoo-pre-foodcalorie-<ts>.db`；`foodcalorie-api/.env.bak.<ts>`；`gakiwoo.com.bak.<ts>`
2. foodcalorie-api/.env：JWT_SECRET ← 与 `/etc/gakiwoo/api.env` 同源（服务器端 shell 复制，全程不显示值）、DB_PATH ← 共享库；pm2 restart（id 37 online）
3. Nginx `gakiwoo.com` server 块新增 `location ^~ /api/v1/ { proxy_pass 127.0.0.1:3001; }`（^~ 最长前缀优先于 `/api/` → 3000）；nginx -t + reload 通过
4. 外网验证：`https://gakiwoo.com/api/v1/health` 200、`/api/auth/me` 401、`/api/v1/records` 401 ✅

### 互通验证（全部通过）
- 登录 → cookie jar 捕获双 token → Bearer → 记录 CRUD/stats/calendar 全通；gakiwoo `/api/auth/me` 认我们的 token（双向互通）；`Cookie: refresh_token=` 头 refresh 200（移动端模式）
- 共享库中业务表已建（user_profiles/food_records/food_items/favorites/contents/challenges），测试数据已清理

### 修复的 bug
- PUT 部分更新失败（recordBody 全量校验）→ routes 改 `validate(recordBody.partial())` + service.updateRecord 以现有记录为基底 merge（支持 PATCH 语义）
- 前端 refreshSession 原发 body `{refresh_token}`（gakiwoo 不认）→ 改为纯 cookie 通道（Web 浏览器自动携带；移动端手动 Cookie 头）

### 前端联调（E2E 8/8 通过，puppeteer）
- vite.config.js 代理改指 `https://gakiwoo.com`（/api/auth、/api/v1），新增 dev 专用 cookieRewrite（剥离 Domain/Secure，否则 localhost 拒存 cookie 导致本地登录失效）
- 真实流程：注册→登录→`/me=200`→记录 API 200→退出→`/me=401` 全通；截图 archive/verify-screenshots/verify5/

### 经验
- **paramiko exec_command 的 out 变量易被后续命令覆盖**（nginx_deploy 踩坑：备份命令输出覆盖了 cat 结果，锚点永远找不到）；关键输出用独立变量保存
- **python -c 内嵌反引号/`$()` 会被 bash 命令替换破坏** → 文档更新用 Edit/Write 工具，不要用 python -c 拼 shell
- npm 本机网络受限依旧（单包 90s+），依赖在服务器安装（226 包秒装）；本机 test/ 单测待网络恢复后补跑
- 含服务器凭据的接入脚本已全部归档 archive/scripts/（inspect5-12/integrate_env/verify_interop*/deploy_fix/nginx_deploy/diag_*），建议后续删除

## M4 业务域开发（同日追加，已上线验证 19/19）

### 新增模块（backend/src/modules/，全部部署服务器 + 真实 token 验证通过）
- **profiles**（M4）：GET/PUT `/api/v1/foodcalorie/profile`——无档自动建默认档、zod 全字段部分更新、boolean→0/1、昵称读共享 users 表
- **foods**（M5）：GET `/foods?keyword=&category=`、`/foods/categories`——43 条种子食物库
- **contents**（M5）：GET `/contents?type=article|recipe`、`/contents/:id`（详情 +1 浏览量）——6 条种子（食谱含 ingredients/steps JSON）
- **favorites**（M5）：GET/POST/DELETE `/favorites`（type=recipe|article|food，UNIQUE 防重 409）
- **challenges**（M6）：GET `/challenges`（含我的进度）+ POST `/:id/join` + `/:id/checkin`（每日 +1 天/+10 分，同日重复 429）；新增 `challenge_participants` 表；cnToday() 用 UTC+8
- **export**（M6）：POST `/export?format=csv|json&range=day|week|month|all`——CSV 附件下载（BOM 防乱码）

### 关键经验（新增）
- **better-sqlite3 命名参数缺失即抛 RangeError**（食谱种子缺 views 字段 → contents 整表种子失败且被事务回滚，SQLite 表却仍存在 → 表象是"表在但数据 0 条"）→ 种子行统一 `{...BASE_DEFAULTS, ...row}` 归一化
- 验证脚本里"自动建档校验 target_calories===1400"是顺序伪影：上一轮 PUT 已改成 1500 → 复跑时校验不等于默认值，属测试设计问题非 bug

### M7 前端对接字段映射（已记录待办）
设置页各页 → profile PUT；发现页 → contents GET；搜索页 → foods GET；收藏页 → favorites；挑战页 → challenges；「我的记录」统计 → records/stats

