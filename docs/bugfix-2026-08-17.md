# 全栈 Bug 诊断与修复报告（2026-08-17）

> 范围：backend 9 模块 + frontend 31 页面 + API/共享层全量审查。
> 结论：确认并修复 **13 处**（后端 5 + 前端 8），新增 7 个回归用例，服务器单测 **45/45 通过**，生产 E2E 通过。

## 修复清单

### 后端

| # | 优先级 | 位置 | 问题 | 修复 |
|---|--------|------|------|------|
| 1 | P1 | `records/routes.js` | `month=2026-13` 通过 regex 校验后 `Date` 得 NaN，`toISOString()` 抛 `RangeError` → 500 | regex 收紧为 `^\d{4}-(0[1-9]\|1[0-2])$` → 400 |
| 2 | P2 | `records/export/routes.js` + `shared/utils/date.js` | `date=2026-02-31` 被 JS Date 静默回滚到 3 月，统计/导出返回错误月份的数据 | 新增共享 `isValidCnDate()`，zod refine 校验真实日历日 → 400 |
| 3 | P2 | `challenges/service.js` | join/checkIn 不校验挑战有效期，已结束挑战可无限打卡刷积分 | 新增 `assertWithinWindow()`（`[start_date, end_date]` 窗口校验，未开始/已结束 → 400） |
| 4 | P2 | `shared/uploads/imageStore.js` | `cleanupExpiredPending` 中单文件 `unlinkSync` 抛非 ENOENT（EPERM/EBUSY）时异常上抛，阻断当前用户上传并误删新文件 | 逐文件 try/catch，失败仅记日志 |
| 5 | P2 | `server.js` | 启动日志打印 `/api/v1/health`，实际挂载在 `/api/v1/foodcalorie/health`，按日志探测会误判 | 日志路径修正 |

### 前端

| # | 优先级 | 位置 | 问题 | 修复 |
|---|--------|------|------|------|
| 6 | P1 | `FoodCalorie-Me.jsx` | `fetchMe()` 已返回 `user` 对象，组件再取 `me.user` 恒 undefined，昵称/邮箱永远显示"未设置" | `setUser(me)` |
| 7 | P1 | `src/api/client.js` | 401 刷新无单飞：并发请求同时触发多次 refresh，若服务端轮换 refresh cookie 后发刷新失败 → 误踢下线 | 共享单个 refresh Promise（single-flight） |
| 8 | P1 | `src/api/client.js` | 游客（从未登录）任意请求 401 即被 `location.assign` 整页踢到登录页，Home/Me 的游客降级 UI 成死代码 | 仅"曾登录但会话失效"（本地存在过 token）才跳转；游客直接抛 401 由页面渲染游客视图 |
| 9 | P1 | `AddFood/Search.jsx` | 防抖搜索无竞态守卫：快速输入时慢的旧关键词响应返回会覆盖新结果 | 请求序号守卫，过期响应丢弃 |
| 10 | P2 | `client.js` | 登录/注册页豁免用 `pathname` 判断；APK 用 HashRouter，pathname 恒为 `/index.html`，豁免永久失效 | `onAuthPage()` 同时兼容 pathname 与 hash |
| 11 | P2 | `RecordsMonth.jsx` | 当月日均 = 月总热量 ÷ 整月天数，未来日期计入分母，月中查看日均被系统性低估 | 当月分母取 `min(今天, 月末)` |
| 12 | P2 | `RecordsWeek.jsx` / `CameraResult.jsx` / `App.jsx` | 重试按钮整页 reload 丢 SPA 状态；`confidence` 缺失渲染 "NaN%"；自维护返回栈 Tab 切换堆积重复项 | 重试改重发请求；`?? 0` 兜底；栈按 `lastIndexOf` 截断去重 |

## 回归测试（新增 7 用例）

- `records.test.js`：`month=2026-13` → 400、`month=2026-00` → 400、`date=2026-02-30` → 400、导出 `date=2027-02-29` → 400、列表 `date=2026-02-31` → 400
- `challenges.test.js`：已结束挑战 join → 400、未开始挑战 checkIn → 400（`test.before` 拉长种子窗口适配）

## 验证记录

- 本地：后端全部改动文件 `node --check` 通过；日期工具函数单测通过；前端 vitest 14/14
- 服务器（Node 20）：`node --test` **45/45 通过**（4.5s）
- 生产：`https://foodcalorie.gakiwoo.com` 部署后 E2E 冒烟通过

## 审查确认无问题的项（抽查实测）

越权防护（用户 A 无法读写用户 B 数据）、JWT 过期处理、分页 offset 计算、打卡幂等（同日 429）、收藏去重、CSV 公式注入防护、rateLimit 滑窗（无内存泄漏）、周区间计算（周一开头）、`record_time` 文本切片无时区问题、ProtectedImage objectURL 清理。

## 遗留待办（未在本轮处理）

- multer 1.x → 2.x 升级（已知漏洞，需回归上传链路）
- Redis 多实例限流（当前单机内存滑窗）
- `loginThrottle` 死代码清理（登录在 gakiwoo-api 侧）
